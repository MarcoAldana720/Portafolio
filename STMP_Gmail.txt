// Codigo para poder enviar información de contacto a Gmail
$mail->isSMTP();
      $mail->Host = 'smtp.gmail.com';
      $mail->SMTPAuth = true;
      $mail->Username = 'aldanaley720@gmail.com';
      $mail->Password = 'uefcatljaxybatov';
      // $mail->Password   = 'gnsldqicyuzfsnli';
      $mail->SMTPSecure = 'tls';
      $mail->Port = 587;

      // Remitente y destinatario
      $mail->setFrom($email, $name);
      $mail->addAddress('aldanaley720@gmail.com', 'Mi Sitio Web');